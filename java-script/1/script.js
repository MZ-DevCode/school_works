let button = document.getElementById("button");
let result = documnet.getElementById("resultText");

let click = 0;

button.addEventListener('click', () =>{
	click++
	text.textContent = 'Click count: ' + clickCount;

});
